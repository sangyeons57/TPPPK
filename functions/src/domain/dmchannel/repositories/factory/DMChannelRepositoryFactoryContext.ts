import {RepositoryFactoryContext} from "../../../shared/RepositoryFactory";

/**
 * Context for DM channel repository factory
 */
export interface DMChannelRepositoryFactoryContext extends RepositoryFactoryContext {
  // No additional context properties needed for now
  // This interface exists for future extensibility
}