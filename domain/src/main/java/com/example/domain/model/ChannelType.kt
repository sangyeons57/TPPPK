// 경로: domain/model/ChannelType.kt (ProjectSettingViewModel, EditChannelViewModel 등 기반)
package com.example.domain.model

/**
 * 채널 타입을 나타내는 열거형 (도메인 모델)
 */
enum class ChannelType { TEXT, VOICE }

// 필요시 다른 타입 추가 (예: 공지 채널, 스테이지 채널)